import 'package:flutter/material.dart';

class HomeAppliances extends StatelessWidget {
  const HomeAppliances({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('home apliances'),
      ),
    );
  }
}
